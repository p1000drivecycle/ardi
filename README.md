# Squint v1.1.0: Added image transport node and relay camera_info topic

## ROS2 Jazzy/Gazebo Harmonic:
- diff_drive sensor plugin
- imu sensor plugin
- rgbd_camera (via sensors plugin)


## Install plugins/tools:
- sudo apt ros-jazzy-diff-drive-controller
- sudo apt install ros-jazzy-rviz-imu-plugin
- sudo apt install ros-jazzy-compressed-image-transport
- sudo apt install ros-jazzy-theora-image-transport
- sudo apt install ros-jazzy-zstd-image-transport
- sudo apt install ros-jazzy-topic-tools

